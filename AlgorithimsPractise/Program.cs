
using DataStructures.Arrays.Medium;
using DataStructures.Arrays.HackerRank;
using DataStructures.Arrays.Easy;
using DataStructures.Arrays.Hard;
using DataStructures.Arrays.LeetCode;
using DataStructures.Strings.Easy;
using DataStructures.Strings.Meduim;
using static System.Console;

namespace AlgorithimsPractise
{
	class Program
	{
		static void Main(string[] args)
		{

			// RunLengthEncoding.Run();
			GroupAnagrams.Run();
			

			 //FirstDuplicateValue.Run();
			//ValidPalindrome.Run();

			/**
			char first = 'a';
			char last = 'z';

			WriteLine((int)first);
			WriteLine((int)last);
			**/
			//CeaserCipherEncryptor.Run();
			//MinimumRewards.Run();
			//LongestRange.Run();
			//FourNumberSum.Run();
			//SubarraySort.Run();
			//TwoNumberIndices.Run();

			//ThreeNumberSum.Run();
			//PalindromeCheck.Run();



			//MonotonicArray.Run();
			//SpiralTraverse.Run();
			//ReverseIntegerArray.Run();
			//LongestPeak.Run();
			//ArrayOfProducts.Run();

			//.RunSort();
			//SortType.RunBubbleSort();
			//SortType.RunSelectionSort();
			/**
		var tree = new BinarySearchTree();

		tree.PreOrderTraversal(tree.Root);
		while (true) {
			int tofind = Convert.ToInt32(ReadLine());
			var node = tree.Lookup(tofind);
			if(node != null)
				WriteLine($"found node with value '{node.Value}'");
			else
				WriteLine($"value {tofind} not found!, Try another");
		}
		**/

			//TwoNumberSumTest.Run();
			//ValidateSubSequence.Run();
			//ThreeNumberSumTest.Run();		
			//MoveElementToEndPractice.Run();

			/**
			var one = new Node(1);

			var ddList = new DoublyLinkedList(one);
			ddList.SetTail(new Node(2));
			ddList.SetTail(new Node(3));
			ddList.SetTail(new Node(4));
			ddList.SetTail(new Node(5));
			ddList.SetHead(new Node(4));
			ddList.SetTail(new Node(6));

			ddList.InsertAtPosition(2, new Node(18));

			ddList.PrintListValues();
			**/

		}
	

		
	}
}
